# Browser Graph Stub

Phase 9 belongs here. The graph should:

- Fetch `GET /graph/{repo_id}`.
- Render nodes and weighted co-change edges with D3.
- On click, fetch `GET /trace/{repo_id}/{file_path}` for an explanation panel.
